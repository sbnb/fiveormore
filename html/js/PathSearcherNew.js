/*
    Search for valid paths on a board.
*/

function PathSearcherNew(logicalBoard) {
}

// Return the list of cells between startCell and including targetCell
PathSearcherNew.prototype.search = function (startCell, targetCell) {
    return [];
}
